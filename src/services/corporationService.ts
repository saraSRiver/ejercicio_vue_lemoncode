import {Corporation} from '../types/Corporation';
/*
export const corporationService= {
    async get(): Promise<Corporation[]> {
      const corps= await fetch(`https://api.github.com/orgs/${orgs}/members`).then((c) => c.json);
      return corps;
    },
  };
*/